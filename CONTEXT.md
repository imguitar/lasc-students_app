# สรุปบริบทและสถานะโปรเจกต์ (Handover Context จาก Antigravity)

## 1. Tech Stack และข้อกำหนดด้าน UI/Design
- สแตก: React + Tailwind CSS + Vite + Lucide Icons + Node.js (Express) + MySQL
- ธีมสีหลัก: Modern Minimalist "ม่วง-ขาว" (Violet-White)
  - สีหลัก: `violet-600`, `violet-50/100` (พื้นหลัง/เส้นขอบ)
  - สีเน้น/สถานะ: อำพัน/ทอง (`amber`), เขียว (`emerald`)
  - ข้อบังคับเข้มงวด: ห้ามใช้อิโมจิทุกจุดเด็ดขาด ให้ใช้เฉพาะ SVG Icons (Lucide) เท่านั้น และเลิกใช้ปุ่มสีดำทึบ

## 2. สิ่งที่ปรับปรุงและทำไปแล้ว (Refactored Components)
- Navbar:
  - เพิ่ม Widget ปฏิทินและเวลาจริงแบบเดินสดบน Navbar (`hidden md:flex rounded-full bg-violet-50/60 border border-violet-100/80`)
  - User Profile Trigger: สถานะปกติขอบสีเทาอ่อน (`border-slate-200`) เมื่อกดเปิดถึงจะขึ้นขอบสีม่วง (`border-2 border-violet-300`)
  - หน้าแรก (/coop): ลบปุ่มสีดำและตัวย่อวงกลมดำออก นำ Notification Bell และ Profile Trigger ตัวใหม่ไปใส่แทน พร้อมปุ่มนำทางไปแดชบอร์ด
- Sidebar:
  - แก้ปัญหาข้อความตัดตกบรรทัด โดยขยายขนาดความกว้างเป็น `w-64` / `w-[260px]` และใส่ `whitespace-nowrap`
- หน้าจอและการ์ด:
  - ปรับการ์ดคำร้องล่าสุด, ฟอร์มยื่นคำร้องใหม่ (รองรับ Mobile ไม่ให้อึดอัด), และกล่อง Empty State ให้เข้าธีมโค้งมน `rounded-[28px]` โทนม่วง-ขาว
- การเปิดดูเอกสาร:
  - ยกเลิก Dialog/Modal พรีวิวไฟล์เอกสารเดิมทั้งหมด
  - บนคอมพิวเตอร์: ให้เปิดไฟล์ PDF แท็บใหม่ผ่าน Native Browser Viewer (`window.open`)
  - บนมือถือ: ให้สั่งดาวน์โหลดไฟล์ลงเครื่องตรงทันที

## 3. กฎเกณฑ์และ Logic การทำงานของระบบ (Business Logic & Workflows)
- การแสดงกำหนดการ:
  - หากแอดมินยังไม่ได้ระบุกรอบเวลา ให้ซ่อนข้อมูล Mockup ทั้งหมด และแสดง Empty State แจ้งว่า "ยังไม่มีการประกาศกำหนดการ"
- หนังสือขอความอนุเคราะห์ (Pre-Approval):
  - เปลี่ยนชื่อหัวข้อ Modal เป็น "หนังสือขอความอนุเคราะห์ขอฝึกประสบการณ์"
  - เพิ่มเมนู "ดู QR ตอบรับล่วงหน้า" ใน Actions Dropdown ของตารางแอดมินแดชบอร์ด
- ขั้นตอนการตอบรับของสถานประกอบการ (Company Response):
  - เมื่อบริษัทกด "ตอบรับ": ให้เปิด Modal ที่มี (1) สิ่งที่อยากให้นักศึกษาเตรียมตัว (2) Canvas วาดลายเซ็นดิจิทัล (3) อีเมลติดต่อของสถานประกอบการ
  - บันทึกและจดจำอีเมลของสถานประกอบการลง Database
  - สำคัญมาก: เมื่อบริษัทกดส่ง ให้ส่งคำร้องกลับมาหา "แอดมินเท่านั้น" (Status: `COMPANY_ACCEPTED` / รอแอดมินออกใบส่งตัว) ห้ามเปลี่ยนเป็นออกฝึกงานหรือส่งต่อไปยังอาจารย์โดยพลการ
- การคำนวณสถานะและส่งใบส่งตัว (Date-driven Status & Referral Dispatch):
  - แอดมินเป็นผู้กำหนดวันออกฝึก (startDate, endDate) และแนบไฟล์ใบส่งตัวด้วยตนเอง
  - เมื่อแอดมินกดยืนยัน นักศึกษาสามารถดาวน์โหลดใบส่งตัวได้ทันที
  - สถานะการฝึกงานจะเปลี่ยนตาม "วันที่จริงของระบบ" เท่านั้น:
    - วันนี้ < startDate: "อนุมัติแล้ว (รอออกฝึกงาน)"
    - startDate <= วันนี้ <= endDate: "กำลังออกฝึกงาน"
    - วันนี้ > endDate: "สิ้นสุดการฝึกงาน (รอประเมิน)"

กรุณายึดโครงสร้างและเงื่อนไขทั้งหมดนี้เป็นหลักในการพัฒนาฟังก์ชันต่อไป